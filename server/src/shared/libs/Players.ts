import { Player } from './Player';

export class Players {
  // players: Player[];
  // constructor(players: Player[] = []) {
  //   this.players = players;
  // }
  // *[Symbol.iterator]() {
  //   for (const player of this.players) {
  //     yield player;
  //   }
  // }
  // get size() {
  //   return this.players.length;
  // }
  // add(player: Player) {
  //   this.players.push(player);
  // }
  // getIndex(player: Player) {
  //   return this.players.findIndex((plr) => plr.id === player.id);
  // }
  // prev(curPlayer: Player) {
  //   const idx = this.getIndex(curPlayer);
  //   if (idx === -1) {
  //     return null;
  //   }
  //   return idx === 0 ? this.players[this.size - 1] : this.players[idx - 1];
  // }
  // next(curPlayer: Player) {
  //   const idx = this.getIndex(curPlayer);
  //   if (idx === -1) {
  //     return null;
  //   }
  //   return this.players[(idx + 1) % this.size];
  // }
}
