/**
 * Unknown - Disconnect from server, leave from room, etc.
 */
export const enum TypePlayerRole {
  Attacker = 'Attacker',
  Defender = 'Defender',
  Waiting = 'Waiting',
  Unknown = 'Unknown',
}
